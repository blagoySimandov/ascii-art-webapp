apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "img-ascii.fullname" . }}-test-connection"
  labels:
    {{- include "img-ascii.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "img-ascii.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
