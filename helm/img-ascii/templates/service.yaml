apiVersion: v1
kind: Service
metadata:
  name: {{ include "img-ascii.fullname" . }}
  labels:
    {{- include "img-ascii.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "img-ascii.selectorLabels" . | nindent 4 }}
